spring:
  jpa:
    properties:
      hibernate:
        format_sql: true
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: test
            client-secret: test
            scope: email

logging.level:
  org.hibernate.SQL: debug

server:
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true